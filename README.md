
  # Create Flipbook with Links

  This is a code bundle for Create Flipbook with Links. The original project is available at https://www.figma.com/design/m1aNip3izmn2Y0OOa5cxyP/Create-Flipbook-with-Links.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  